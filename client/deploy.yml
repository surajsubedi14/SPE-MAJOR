---
- hosts: localhost
  tasks:
    - name: Pull Docker image from Docker Hub
      shell: docker pull surajiiitb/ecommercefrontend:latest



    - name: Run Docker container
      shell: docker run -d -p 8081:8080 surajiiitb/ecommercefrontend:latest